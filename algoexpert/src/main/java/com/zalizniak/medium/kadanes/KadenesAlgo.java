package com.zalizniak.medium.kadanes;

/**
 * Maximum Sum of Subarray
 * The maximum subarray problem is the task of finding:
 * - the contiguous subarray
 * - within a one-dimensional array, a[1…n]
 * - of numbers which has the largest sum
 */
public interface KadenesAlgo {

    public int maxSum(Integer[] in);
}
